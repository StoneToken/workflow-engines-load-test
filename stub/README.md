# Stub REST API for tests

[Apisprout](https://github.com/danielgtaylor/apisprout) is used as a REST API mock server with pet store [OpenAPI](https://petstore.swagger.io/). It is deployed using docker-compose.

```shell
docker-compose up
```